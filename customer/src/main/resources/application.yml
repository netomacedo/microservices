server:
  port:8080

spring:
  application:
    name: customer
  datasource:
    password: '12345'
    url: jdbc:postgresql://localhost:5432/customer
    username: 'admin'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
      show-sql: 'true'
